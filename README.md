# FlashLink Signaling Server

This is the signaling server for the FlashLink Android application. It handles WebRTC handshakes (SDP and ICE candidates) via WebSockets.

## Setup
1. `npm install`
2. `node server.js`
